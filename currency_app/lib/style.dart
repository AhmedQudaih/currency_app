import 'package:flutter/material.dart';

const spinColor = Colors.blue;
const spinSize = 50.0;

const appBarShape = RoundedRectangleBorder(
  borderRadius: BorderRadius.vertical(
    bottom: Radius.circular(80),
  ),
);

const bottomBarShape = AutomaticNotchedShape(
  RoundedRectangleBorder(
    borderRadius: BorderRadius.vertical(
      top: Radius.circular(80),
    ),
  ),
);

const bottomBarColor = Colors.blue;

const centerTextStyle = TextStyle(
  fontSize: 25,
);

const iconButtonSize = 30.0;
const iconButtonColor = Colors.white;

const swapIconeSize = 30;

const dropdownButtonStyle = TextStyle(
  color: Colors.white,
);
